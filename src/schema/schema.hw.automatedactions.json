{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://wie-niet.nl/doorlockd/schema.automated_actions.json",
  "title": "Automates actions object",
  "description": "bla bah dummy",
  "type": "object",
  "properties": {
      "config_name": {
          "description": "config_name name",
		  "readOnly": true,
          "type": ["string"]
      },
      "log_name": {
          "description": "log_name name",
		  "readOnly": true,
          "type": ["string"]
      },
      "counter": {
          "description": "trigger counter",
		  "readOnly": true,
          "type": ["integer"]
      },
      "trigger_actions": {
          "description": "actions taken when button is triggered",
          "type": ["array"]
      },
      "status": {
          "description": "current status, change to trigger ",
          "type": ["boolean"]
      },
      "default_status": {
          "description": "default status",
          "type": ["boolean"]

	  }
  }, 
  "required": [ "status"],
  "additionalProperties": false
}