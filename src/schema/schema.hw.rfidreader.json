{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://wie-niet.nl/doorlockd/schema.dummy.json",
  "title": "Hardware RFID Reader object",
  "description": "bla bah dummy",
  "type": "object",
  "properties": {
      "config_name": {
          "description": "config_name name",
		  "readOnly": true,
          "type": ["string"]
      },
      "log_name": {
          "description": "log_name name",
		  "readOnly": true,
          "type": ["string"]
      },
      "counter": {
          "description": "trigger counter",
		  "readOnly": true,
          "type": ["integer"]
      },
      "spi_bus": {
          "description": "spi_bus number for rc522",
		  "readOnly": true,
          "type": ["number"]
      },
      "spi_device": {
          "description": "spi_device number for rc522",
		  "readOnly": true,
          "type": ["number"]
      },
      "pin_irq": {
          "description": "pin_irq name for rc522",
		  "readOnly": true,
          "type": ["string"]
      },
      "pin_rst": {
          "description": "pin_irq name for rc522",
		  "readOnly": true,
          "type": ["string"]
      },
      "path": {
          "description": "connection path for nfcpy",
		  "readOnly": true,
          "type": ["string"]
      },
      "status": {
          "description": "current status, change to trigger",
          "type": ["boolean"]
      },
      "default_status": {
          "description": "default status",
          "type": ["boolean"]

	  }
  }, 
  "required": [ "status"],
  "additionalProperties": false
}