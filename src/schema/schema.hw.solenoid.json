{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://wie-niet.nl/doorlockd/schema.dummy.json",
  "title": "Hardware Solenoid object",
  "description": "bla bah dummy",
  "type": "object",
  "properties": {
      "config_name": {
          "description": "config_name name",
          "type": ["string"]
      },
      "gpio_pin": {
          "description": "gpio_pin name",
          "type": ["string"]
      },
      "time_wait": {
          "description": "time in seconds",
          "type": ["number"]
      },
      "status": {
          "description": "Some number ",
          "type": ["boolean"]

	  }
  }, 
  "required": [ "config_name"],
  "additionalProperties": false
}